[project]
name = "jinx"
version = "0.1.0"
description = "Simulation-first advisory cognitive integration platform foundation."
requires-python = ">=3.11"
readme = "README.md"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
